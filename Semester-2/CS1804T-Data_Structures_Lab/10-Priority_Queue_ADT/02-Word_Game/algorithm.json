[
  {
    "algorithm": "Group String Scoring",
    "input": [
      "v - A 2D vector of strings, where v[i] represents the i-th group (i=0, 1, 2) and v[i][j] is the j-th string in that group.",
      "num1 - The number of strings in each group."
    ],
    "output": [
      "scores - A vector of 3 integers, where scores[i] is the calculated score for group i."
    ],
    "steps": [
      {
        "step": "Initialize an empty integer vector `scores` to store the results for the three groups."
      },
      {
        "step": "Loop for each group `i` from 0 to 2:",
        "substeps": [
          {
            "step": "Initialize the score for the current group: `current_group_score = 0`."
          },
          {
            "step": "Loop for each string `current_string = v[i][j]` in the current group `i` (loop index `j` from 0 to `num1 - 1`):",
            "substeps": [
              {
                "step": "Initialize a counter for occurrences in other groups: `occurrence_count = 0`."
              },
              {
                "step": "Determine the indices of the other two groups: `other1 = (i + 1) % 3`, `other2 = (i + 2) % 3`."
              },
              {
                "step": "Loop through each string `s` in group `other1` (index `k` from 0 to `num1 - 1`):",
                "substeps": [
                  {
                    "step": "If `v[other1][k]` is equal to `current_string`:",
                    "substeps": [
                        {
                           "step": "Increment `occurrence_count`."
                        }
                    ]
                  }
                ]
              },
              {
                "step": "Loop through each string `s` in group `other2` (index `k` from 0 to `num1 - 1`):",
                "substeps": [
                  {
                    "step": "If `v[other2][k]` is equal to `current_string`:",
                     "substeps": [
                        {
                           "step": "Increment `occurrence_count`."
                        }
                    ]
                  }
                ]
              },
              {
                "step": "Evaluate the `occurrence_count` for `current_string`:",
                "substeps": [
                  {
                    "step": "If `occurrence_count == 1` (string exists in exactly one other group):",
                     "substeps": [
                        {
                           "step": "Add 1 to `current_group_score`."
                        }
                    ]
                  },
                  {
                    "step": "If `occurrence_count == 0` (string is unique to group `i`):",
                     "substeps": [
                        {
                           "step": "Add 3 to `current_group_score`."
                        }
                    ]
                  }
                ]
              }
            ]
          },
          {
            "step": "After iterating through all strings in group `i`, add the calculated `current_group_score` to the `scores` vector."
          }
        ]
      },
      {
        "step": "After the outer loop (i) completes, the `scores` vector holds the final scores for the three groups."
      }
    ]
  }
]
